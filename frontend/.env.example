# URL base da API. Em dev, o proxy do Vite (vite.config.ts) ja encaminha
# /api e /uploads para o backend em localhost:3333, entao esta variavel
# so precisa ser definida em builds de producao com frontend/backend
# servidos de origens diferentes.
VITE_API_URL=/api
